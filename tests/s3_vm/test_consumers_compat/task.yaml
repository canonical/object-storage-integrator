summary: test_consumers_compat
environment:
  TEST_MODULE: test_consumers/test_compat.py
  SUBSTRATE: vm
execute: |
  cd "$SPREAD_PATH/s3"
  tox run -e integration -- "tests/integration/$TEST_MODULE" -x
